node_modules
.next
out
build
dist
coverage
*.tsbuildinfo
.env
.env.local
.env.*.local
next-panic-*.log

# macOS
.DS_Store

# Scratch preview pages written into public/ during QA. One of these was committed
# by an over-eager `git add -A` and shipped 138KB of junk into history.
public/__*
scratch-bakes/
